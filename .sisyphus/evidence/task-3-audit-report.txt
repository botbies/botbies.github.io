Task 3 Audit Report: build.js pageShell / color-style audit

Results
- `grep -n "pageShell" build.js` found 5 matches total.
  - 1 definition: `function pageShell(...)`
  - 4 call sites: home, post, tag, author generators
- `grep -n "writeFileSync.*html" build.js` found 4 matches total.
  - `index.html` for home
  - `index.html` for posts
  - `index.html` for tags
  - `index.html` for authors
- Every HTML write goes through a generator that returns `pageShell()`. Match confirmed.

Color / style checks
- `grep -n 'style=' build.js` → 0 matches
- `grep -n '<style' build.js` → 0 matches
- `grep -niE 'bgcolor|color=|style=.*color' build.js` → 0 matches
- `grep -n "comment-card\|renderComments" build.js` found:
  - `renderComments()` definition
  - usage inside `generatePost()`
  - `renderComments()` emits `class="comment-card ..."` only; no inline styles

Conclusion: CLEAN
- All HTML pages are generated through `pageShell()`.
- No hardcoded inline color styles or legacy color attributes were found in `build.js`.
